name: Cppcheck
on: [ push, pull_request ]
jobs:
  cppcheck:
    name: Cppcheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: apt install cppcheck make
        name: Install dependencies
      - run: make testCppcheck
